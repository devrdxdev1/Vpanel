```
sudo apt update

sudo apt install git -y

git clone https://github.com/devrdxdev1/Vpanel.git

sudo apt install python3-pip -y

(Check folder are there by this) ls -l

pip install -r requirements.txt

python3 hvm.py

pip3 install flask

pip3 install flask-socketio

pip3 install flask_login

pip3 install docker
```
